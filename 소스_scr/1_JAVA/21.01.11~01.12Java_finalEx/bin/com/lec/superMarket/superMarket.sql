DROP SEQUENCE CUSTOMER_SEQ;
CREATE SEQUENCE CUSTOMER_SEQ
    START WITH 1 
    INCREMENT BY 1
    MAXVALUE 999999
    MINVALUE 1 
    NOCYCLE
    NOCACHE;
DROP TABLE CUS_LEVEL;
DROP TABLE CUSTOMER ;

CREATE TABLE CUS_LEVEL(
    levelNO NUMBER(1)PRIMARY KEY ,
    levelNAME VARCHAR2(20),
    LOW NUMBER(9),
    HIGH NUMBER(9)
     
);
CREATE TABLE CUSTOMER(
    cID NUMBER(6) PRIMARY KEY,
    cTEL VARCHAR2(20) NOT NULL,
    cNAME VARCHAR2(30) NOT NULL,
    cPOINT NUMBER(9)DEFAULT 1000,
    cAMOUNT NUMBER(9)DEFAULT 0,
    levelNO NUMBER(1)DEFAULT 1,
    FOREIGN KEY (levelNO) REFERENCES CUS_LEVEL(levelNO)
);
--TEST용 데이트 입력
INSERT INTO CUS_LEVEL VALUES(1,'NORMAL',0,999999);
INSERT INTO CUS_LEVEL VALUES(2,'SILVER',1000000,1999999);
INSERT INTO CUS_LEVEL VALUES(3,'GOLD',2000000,2999999);
INSERT INTO CUS_LEVEL VALUES(4,'VIP',3000000,3999999);
INSERT INTO CUS_LEVEL VALUES(5,'VVIP',4000000,4999999);
SELECT * FROM CUS_LEVEL;
SELECT * FROM CUSTOMER;

INSERT INTO CUSTOMER(cID,cTEL,cNAME) VALUES(CUSTOMER_SEQ.NEXTVAL,'010-9999-9999','홍길동');
INSERT INTO CUSTOMER VALUES (CUSTOMER_SEQ.NEXTVAL,'010-8888-8888','김길동',1000,5000000,5);

-- 0. 레벨이름들 검색 : public Vector<String> getLevelNames()
SELECT LEVELNAME FROM CUS_LEVEL;

-- 1. cId(아이디)검색 : public CustomerDto cIdGetCustomers(String cId)
-- cIdGetCustomers의 메소드를 만들어야함! 
SELECT * FROM CUSTOMER;
SELECT * FROM CUS_LEVEL;

SELECT cId,cTEL,cNAME,cPOINT,cAMOUNT,LEVELNAME,
    HIGH-CAMOUNT+1 forLEVELUP
    FROM CUSTOMER C ,CUS_LEVEL L
    WHERE c.levelno=l.levelno
    AND CID=1;--최고 레벨인 고객도 나와서 수정! 
    
SELECT cId,cTEL,cNAME,cPOINT,cAMOUNT,LEVELNAME,
    (SELECT HIGH-CAMOUNT+1 FROM CUSTOMER WHERE CID=C.CID AND LEVELNO<>5 ) forLEVELUP
    FROM CUSTOMER C ,CUS_LEVEL L
    WHERE c.levelno=l.levelno
    AND CID=1;--자바 사용쿼리
    
    
-- 2. 폰뒤4자리(FULL) 검색 
-- 폰뒷4자리검색= FULL검색
-- public ArrayList<CustomerDto> cTelGetCustomers(String cTel); 자바에서 가지고올떄
-- cTelGetCustomers의 메소드를 만들어야함! 
SELECT cId,cTEL,cNAME,cPOINT,cAMOUNT,LEVELNAME,
    (SELECT HIGH-CAMOUNT+1 FROM CUSTOMER WHERE CID=C.CID AND LEVELNO<>5 ) forLEVELUP
    FROM CUSTOMER C ,CUS_LEVEL L
    WHERE c.levelno=l.levelno
    AND CTEL LIKE '%9999';
    
SELECT cId,cTEL,cNAME,cPOINT,cAMOUNT,LEVELNAME,
    (SELECT HIGH-CAMOUNT+1 FROM CUSTOMER WHERE CID=C.CID AND LEVELNO<>5 ) forLEVELUP
    FROM CUSTOMER C ,CUS_LEVEL L
    WHERE c.levelno=l.levelno
    AND CTEL LIKE '%'||'9999';--자바에서 쓸 SQL

-- 3. 고객이름검색 
--public ArrayList<CustomerDto> cNameGetCustomers(String cName);
SELECT cId,cTEL,cNAME,cPOINT,cAMOUNT,LEVELNAME,
    (SELECT HIGH-CAMOUNT+1 FROM CUSTOMER WHERE CID=C.CID AND LEVELNO<>5 ) forLEVELUP
    FROM CUSTOMER C ,CUS_LEVEL L
    WHERE c.levelno=l.levelno
    AND CNAME='김길동';

-- 4. 포인트로만 구매(1000원짜리를 포인트로만 구매) 
-- public int buyWithPoint(int cAmount)
UPDATE CUSTOMER SET CPOINT=CPOINT -500 WHERE CID=1; --500?    1?

-- 5. 물품구매 (포인트변경, 구매누적금액변경,LEVELNO변경
    --  public int buy(int cAmount, int cId)
    --5-1포인트와 구매누적금액 변경
UPDATE CUSTOMER SET CPOINT=CPOINT+1000000*0.05,
                    CAMOUNT=CAMOUNT+1000000
                    WHERE CID=1;
SELECT * FROM CUS_LEVEL;
    --5-2levelNO변경
SELECT CNAME,CAMOUNT,C.LEVELNO 현재레벨,L.LEVELNO 바꿔야할레벨,LOW,HIGH
    FROM CUSTOMER C,CUS_LEVEL L
    WHERE CAMOUNT BETWEEN LOW AND HIGH; 
    
SELECT L.LEVELNO 
    FROM CUSTOMER C,CUS_LEVEL L
    WHERE CAMOUNT BETWEEN LOW AND HIGH AND CID=1;-- ID가 1인 고객의 바꿔야할; --바꿔야할 레벨

UPDATE CUSTOMER 
    SET LEVELNO =(SELECT L.LEVELNO 
    FROM CUSTOMER C,CUS_LEVEL L
    WHERE CAMOUNT BETWEEN LOW AND HIGH AND CID=1)
    WHERE CID=1;
    
SELECT * FROM CUSTOMER;
COMMIT;
ROLLBACK;
    --5-1+5-2합한 부분을 자바에 쓸 SQL로
        --public int buy(int cAmount, int cid)
UPDATE CUSTOMER SET CPOINT=CPOINT+1000000*0.05,
                    CAMOUNT=CAMOUNT+1000000,
                    LEVELNO =(SELECT L.LEVELNO 
                                FROM CUSTOMER C,CUS_LEVEL L
                                WHERE CAMOUNT+1000000 BETWEEN LOW AND HIGH AND CID=1)      
                    WHERE CID=1;
                    
UPDATE CUSTOMER SET CPOINT=CPOINT+100*0.05,
                    CAMOUNT=CAMOUNT+100,
                    LEVELNO =(SELECT L.LEVELNO 
                                FROM CUSTOMER C,CUS_LEVEL L
                                WHERE CAMOUNT+100 BETWEEN LOW AND HIGH AND CID=1)      
                    WHERE CID=1;

-- 6. 등급별출력
--public ArrayList<CustomerDto> levelNameGetCustomers(String levelName)
--levelNameGetCustomers메소드를 만들어야함 
SELECT cId,cTEL,cNAME,cPOINT,cAMOUNT,LEVELNAME,
    (SELECT HIGH-CAMOUNT+1 FROM CUSTOMER WHERE CID=C.CID AND LEVELNO<>5 ) forLEVELUP
    FROM CUSTOMER C ,CUS_LEVEL L
    WHERE c.levelno=l.levelno AND levelname='VVIP'
    ORDER BY CAMOUNT DESC;
    
-- 7.전체출력 - CID, CTEL, CNAME, CPOINT, CAMOUNT, LEVELNAME, 레벨업을위한쓸돈
--            public ArrayList<CustomerDto> getCustomers()
--getCustomers()라는 메소드를 생성! 
SELECT cId,cTEL,cNAME,cPOINT,cAMOUNT,LEVELNAME,
    (SELECT HIGH-CAMOUNT+1 FROM CUSTOMER WHERE CID=C.CID AND LEVELNO<>5 ) forLEVELUP
    FROM CUSTOMER C ,CUS_LEVEL L
    WHERE c.levelno=l.levelno 
    ORDER BY CAMOUNT DESC;
    
-- 8. 회원가입(고객전화와 고객이름은 입력받아 INSERT)
--          public int insertCustomer(String cTel, String cName)//DTO NO필요
INSERT INTO CUSTOMER (cID,cTEL,CNAME) VALUES(CUSTOMER_SEQ.NEXTVAL,'010-7777-7777','손오공'); 
SELECT * FROM CUSTOMER;

-- 9. 번호수정 : public int updateCustomer(String cTel, int cId)
UPDATE CUSTOMER SET CTEL='010-1111-1111' WHERE  CID=3;

-- 10. 회원탈퇴 : public int deleteCustomer(String cTel)
DELETE FROM CUSTOMER WHERE CID=3;
commit;
SELECT *FROM CUSTOMER;